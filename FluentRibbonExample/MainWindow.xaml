<Window x:Class="FluentRibbonExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FluentRibbonExample"
        xmlns:behavior="clr-namespace:FluentRibbonExample.Behavior"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
        behavior:ViewModelInjectionBehavior.ViewModel="{x:Type local:MainWindowViewModel}"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <Button Content="Zen Window" Command="{Binding NavigateToZenCommand}" />
        <Button Content="Zen Window (using Fluent.Ribbon.Window)" Command="{Binding NavigateToZenAsNewWindowCommand}" />
    </StackPanel>
</Window>